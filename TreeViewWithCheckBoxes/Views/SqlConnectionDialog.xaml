<Window x:Class="GenQ.Views.SqlConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SQL Server Connection Settings" 
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Server -->
        <Label Grid.Row="0" Content="Server:"/>
        <TextBox Grid.Row="1" x:Name="txtServer" Text=".\SQLEXPRESS"/>
        
        <!-- Database -->
        <Label Grid.Row="2" Content="Database:"/>
        <TextBox Grid.Row="3" x:Name="txtDatabase" Text="GenQ_BOQ"/>
        
        <!-- Authentication -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5,0,10">
            <RadioButton x:Name="rbIntegrated" Content="Windows Authentication" 
                         IsChecked="True" Margin="0,0,20,0" 
                         Checked="AuthenticationChanged"/>
            <RadioButton x:Name="rbSqlAuth" Content="SQL Server Authentication"
                         Checked="AuthenticationChanged"/>
        </StackPanel>
        
        <!-- SQL Auth Credentials -->
        <Grid Grid.Row="5" x:Name="pnlCredentials" IsEnabled="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Label Grid.Column="0" Grid.Row="0" Content="Username:"/>
            <TextBox Grid.Column="0" Grid.Row="1" x:Name="txtUsername"/>
            
            <Label Grid.Column="2" Grid.Row="0" Content="Password:"/>
            <PasswordBox Grid.Column="2" Grid.Row="1" x:Name="txtPassword" 
                         Margin="0,5,0,10" Padding="5,3"/>
        </Grid>
        
        <!-- Status -->
        <TextBlock Grid.Row="6" x:Name="txtStatus" 
                   TextWrapping="Wrap" Foreground="Gray"
                   VerticalAlignment="Center"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Test Connection" Width="110" Height="30" 
                    Margin="0,0,10,0" Click="TestConnection_Click"/>
            <Button Content="Save" Width="80" Height="30" 
                    Margin="0,0,10,0" Click="Save_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Height="30" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
